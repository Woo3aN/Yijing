# 2026-06-28 开发日志

## 今日完成
- [x] 新增主题切换功能（设置页 + 持久化 + 即时生效）
- [x] 新增历史记录分页（每页 20 条，搜索分页同步）
- [x] 新增卦象结果复制到剪贴板功能
- [x] 新增卦象结果导出为 .txt 文件功能

## 改动文件

| 文件 | 改动 |
|------|------|
| `src/storage/app_settings.py` | AppSettings 新增 `theme` 字段（默认 "darkly"） |
| `src/main.py` | 启动时从设置读取主题，不再硬编码 |
| `src/ui/settings_page.py` | 新增界面主题下拉框（18 个 ttkbootstrap 内置主题），切换即时生效 |
| `src/storage/history_db.py` | 新增 `get_reading_count()`；`search_readings()` 支持 offset 参数 |
| `src/ui/history_page.py` | 新增分页控件（上一页/下一页/页码显示）；搜索/显示全部时重置页码 |
| `src/ui/divination_page.py` | 新增"复制结果"（系统剪贴板）和"导出文本"（文件保存对话框）按钮 |

## 遇到的问题
1. 原 venv 的 Python 路径指向 Administrator 用户，当前环境不可用 → 新建 venv_local
2. 全局 pip 无法联网安装包 → 新建 venv 时正常安装
3. Windows GBK 编码导致 emoji 字符打印异常 → 用 ASCII 替代验证输出

## 下一步计划
- 考虑加入 AI 流式输出到 UI（`analyze_stream` 已实现但 UI 未对接）
- 考虑加入键盘快捷键（Ctrl+Enter 起卦等）
